name: dashboard_widgets
description: A new Flutter package project.
version: 0.0.1
homepage: null

environment:
  sdk: ">=2.15.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  vrouter: ^1.2.0+11

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

